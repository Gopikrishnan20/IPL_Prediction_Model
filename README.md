# IPL_Prediction_Model
Machine Learning model that predicts the winner of IPL matches using historical match data, incorporating features such as teams, toss result, venue, and city. The model is trained using a Random Forest classifier with a 50–30–20 train, test, and validation split.
